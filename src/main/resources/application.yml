server:
  port: 8888

spring:
  profiles:
    active: native

  cloud:
    config:
      server:
        native:
          search-locations:
            - classpath:/configs/

logging:
  level:
    org.springframework.cloud.config.server: DEBUG